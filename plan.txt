- 1) сделать бд и подключиться к ней
- 2) создать нужные таблицы
Created table of Person:
create table IF NOT EXISTS Person (
    person_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
    full_name varchar(100) UNIQUE  NOT NULL,
    year_of_birth int CHECK ( year_of_birth > 1900 ) NOT NULL
)

Created table of book:
create table Book (
    book_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
    person_id BIGINT REFERENCES person(person_id) ON DELETE SET NULL,
    book_name varchar(100) UNIQUE NOT NULL,
    author_full_name varchar(100) UNIQUE NOT NULL,
    year_of_pub int CHECK ( year_of_pub > 1700 ) NOT NULL
)

- 3) подключить нужные зависимости (postgreSQL, spring core, spring context, spring-web, spring-webmvc, spring-jdbc, thymeleaf-spring5, servlet, hybernate-validator)
- 4) прописать конфигурации для сприга (MyWebAppInitializer, SpringConfig)
- 5) прописать конфигурации для бд
- 6) создать класс человека
- 7) создать класс книги

- 8) Страницы добавления, изменения и удаления человека.
-.добавление
-.изминение
-.удаление
- 9) Страницы добавления, изменения и удаления книги
-.добавление
-.изминение
-.удаление

- 10) Страница со списком всех людей (люди кликабельные - при клике осуществляется
переход на страницу человека).
- 11) Страница со списком всех книг (книги кликабельные - при клике осуществляется
переход на страницу книги).
- 12) Страница человека, на которой показаны значения его полей и список книг, которые он
взял. Если человек не взял ни одной книги, вместо списка должен быть текст "Человек
пока не взял ни одной книги".
- 13) Страница книги, на которой показаны значения полей этой книги и имя человека,
который взял эту книгу. Если эта книга не была никем взята, должен быть текст "Эта
книга свободна".
14) На странице книги, если книга взята человеком, рядом с его именем должна быть кнопка
"Освободить книгу". Эта кнопка нажимается библиотекарем тогда, когда читатель
возвращает эту книгу обратно в библиотеку. После нажатия на эту кнопку книга снова
становится свободно и пропадает из списка книг человека.
15) На странице книги, если книга свободна, должен быть выпадающий список (<select>)
со всеми людьми и кнопка "Назначить книгу". Эта кнопка нажимается библиотекарем
тогда, когда читатель хочет забрать эту книгу домой. После нажатия на эту кнопку, книга
должна начать принадлежать выбранному человеку и должна появится в его списке
книг.
16) Все поля должны валидироваться - с помощью @Valid и Spring Validator, если это
требуется(в том числе добавить вывод ошибки, если библиотекарь пытается удалить несуществующего пользователя).
17) Перевести все на английский и сделать новые скрины.